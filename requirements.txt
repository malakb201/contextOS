# ContextOS v1.0 — Python dependencies
# Install all at once:  python install_deps.py
# Or manually:          pip install -r requirements.txt

# Required on Windows
pywin32==306          # read active window titles (Windows only)
psutil==6.0.0         # CPU/RAM monitoring, process names
pillow==10.4.0        # tray icon image processing
pystray==0.19.5       # system tray icon

# Development / testing
pytest==8.3.3         # run tests: pytest tests/

# Optional — for .exe packaging
# pyinstaller          # pip install pyinstaller
